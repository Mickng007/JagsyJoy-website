---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ProductCard from '../components/ProductCard.astro';
import AnnouncementBar from '../components/AnnouncementBar.astro';
import products from '../data/products.json';

const siteUrl = "https://jagsyjoy-website.pages.dev";
const ogImage = `${siteUrl}/images/blog-1.jpg`;

const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "Jagsy Joy",
  url: siteUrl,
  logo: `${siteUrl}/images/jagsy-joy-logo.png`,
  email: "qenterprises.design@gmail.com",
  description:
    "Jagsy Joy creates screen-free busy boards and toddler learning toys for practical life skills, fine motor development, and hands-on discovery.",
};

const websiteSchema = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  name: "Jagsy Joy",
  url: siteUrl,
  description:
    "Jagsy Joy creates screen-free busy boards and toddler learning toys for practical life skills, fine motor development, and hands-on discovery.",
};
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Jagsy Joy | Screen-Free Busy Boards for Toddlers</title>
    <meta
      name="description"
      content="Discover Jagsy Joy busy boards designed for screen-free play, fine motor skills, practical life learning, and toddler-friendly discovery."
    />
    <link rel="canonical" href={siteUrl} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={siteUrl} />
    <meta property="og:title" content="Jagsy Joy | Screen-Free Busy Boards for Toddlers" />
    <meta property="og:description" content="Discover Jagsy Joy busy boards designed for screen-free play, fine motor skills, practical life learning, and toddler-friendly discovery." />
    <meta property="og:image" content={ogImage} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={siteUrl} />
    <meta property="twitter:title" content="Jagsy Joy | Screen-Free Busy Boards for Toddlers" />
    <meta property="twitter:description" content="Discover Jagsy Joy busy boards designed for screen-free play, fine motor skills, practical life learning, and toddler-friendly discovery." />
    <meta property="twitter:image" content={ogImage} />

    <script type="application/ld+json" set:html={JSON.stringify(organizationSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
  </head>

  <body>
    <AnnouncementBar />
    <Header />

    <main>
      <section class="hero">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>

        <div class="hero-content">
          <p class="eyebrow animate delay-1">Learning Through Play</p>

          <h1 class="hero-title animate delay-2">
            Playful toys for curious little explorers.
          </h1>

          <p class="hero-text animate delay-3">
            Jagsy Joy creates toddler-friendly busy boards designed to support
            fine motor skills, independent play, and everyday discovery.
          </p>

          <div class="hero-actions animate delay-4">
            <a class="button primary" href="/products">Explore Products</a>
            <a class="button secondary" href="/about">Our Story</a>
          </div>
        </div>
      </section>

      <section class="section">
        <div class="section-heading">
          <p class="eyebrow">Featured Products</p>
          <h2>Colorful busy boards made for everyday play.</h2>
          <p>
            Choose from playful toddler busy boards designed for screen-free learning,
            travel time, and hands-on skill practice.
          </p>
        </div>

        <div class="product-grid">
          {products.map((product) => (
            <ProductCard product={product} />
          ))}
        </div>
      </section>

      <section class="skills-section">
        <div class="section-heading">
          <p class="eyebrow">15 Practical Life Skills</p>
          <h2>Busy boards that turn everyday actions into playful learning.</h2>
          <p>
            From zippers and buckles to counting and color matching, Jagsy Joy busy
            boards help toddlers practice real-life skills through hands-on,
            screen-free play.
          </p>
        </div>

        <div class="skills-image-grid">
          <figure class="skills-image-card">
            <img src="/images/pink-busy-board-skills.jpg" alt="Pink busy board practical life skills" />
          </figure>

          <figure class="skills-image-card">
            <img src="/images/blue-busy-board-skills.jpg" alt="Blue busy board practical life skills" />
          </figure>
        </div>
      </section>

      <section class="section why-section">
        <div class="section-heading">
          <p class="eyebrow">WHY CHOOSE JAGSY JOY</p>
          <h2>Learning toys made for curious, growing minds.</h2>
          <p>
            Designed for screen-free discovery, everyday skill-building, and joyful hands-on play.
          </p>
        </div>

        <div class="features-grid">
          <div class="feature-card">
            <div class="feature-icon">📵</div>
            <h3>Screen-Free Play</h3>
            <p>Simple activities that keep toddlers engaged without tablets or phones.</p>
          </div>

          <div class="feature-card">
            <div class="feature-icon">🎒</div>
            <h3>Practical Life Skills</h3>
            <p>Buckles, zippers, laces, buttons, and counting activities help children practice real everyday movements.</p>
          </div>

          <div class="feature-card">
            <div class="feature-icon">✈️</div>
            <h3>Travel Friendly</h3>
            <p>Lightweight busy boards are easy to bring for car rides, trips, restaurants, and quiet playtime.</p>
          </div>

          <div class="feature-card">
            <div class="feature-icon">🌈</div>
            <h3>Toddler-Friendly Design</h3>
            <p>Soft fabric, secure stitching, and colorful activity pieces make learning feel safe, playful, and fun.</p>
          </div>
        </div>
      </section>

      <section class="testimonials-section">
        <div class="section-heading">
          <p class="eyebrow">Testimonials</p>
          <h2>Loved by little learners.</h2>
          <p>
            Real feedback from families using Jagsy Joy for screen-free play, travel,
            and everyday skill-building.
          </p>
        </div>

        <div class="testimonial-carousel-wrap">
          <button
            class="carousel-arrow carousel-arrow-left"
            type="button"
            aria-label="Previous testimonials"
          >
            ‹
          </button>

          <div class="testimonial-carousel" aria-label="Customer testimonials">
            <article class="testimonial-card">
              <img class="testimonial-avatar" src="/images/avatar-amanda.jpg" alt="Amanda R." />
              <p class="stars">★★★★★</p>
              <p class="review">
                “My toddler stayed focused longer than usual. She loved the buckles,
                zipper, and hands-on details.”
              </p>
              <p class="customer">Amanda R.</p>
            </article>

            <article class="testimonial-card">
              <img class="testimonial-avatar" src="/images/avatar-melissa.jpg" alt="Melissa K." />
              <p class="stars">★★★★★</p>
              <p class="review">
                “Perfect for quiet play. We use it during car rides and it keeps my son
                busy without needing a screen.”
              </p>
              <p class="customer">Melissa K.</p>
            </article>

            <article class="testimonial-card">
              <img class="testimonial-avatar" src="/images/avatar-rachel.jpg" alt="Rachel T." />
              <p class="stars">★★★★★</p>
              <p class="review">
                “The colors are bright, the activities are simple, and it feels like a
                fun way to practice everyday skills.”
              </p>
              <p class="customer">Rachel T.</p>
            </article>

            <article class="testimonial-card">
              <img class="testimonial-avatar" src="/images/avatar-nicole.jpg" alt="Nicole S." />
              <p class="stars">★★★★★</p>
              <p class="review">
                “A sweet gift for toddlers. It feels playful, useful, and easy to bring
                along when we travel.”
              </p>
              <p class="customer">Nicole S.</p>
            </article>

            <article class="testimonial-card">
              <img class="testimonial-avatar" src="/images/avatar-jessica.jpg" alt="Jessica M." />
              <p class="stars">★★★★★</p>
              <p class="review">
                “My little one keeps coming back to it. The activities are simple, but
                they hold her attention.”
              </p>
              <p class="customer">Jessica M.</p>
            </article>

            <article class="testimonial-card">
              <img class="testimonial-avatar" src="/images/avatar-lauren.jpg" alt="Lauren P." />
              <p class="stars">★★★★★</p>
              <p class="review">
                “Great for screen-free moments at home. It gives my child something
                calm and hands-on to explore.”
              </p>
              <p class="customer">Lauren P.</p>
            </article>
          </div>

          <button
            class="carousel-arrow carousel-arrow-right"
            type="button"
            aria-label="Next testimonials"
          >
            ›
          </button>
        </div>
      </section>

      <section class="cta">
        <p class="eyebrow">Explore The Collection</p>
        <h2>Find a busy board for everyday learning moments.</h2>
        <a class="button primary" href="/products">Shop Products</a>
      </section>
    </main>

    <Footer />

    <script>
      const carousel = document.querySelector('.testimonial-carousel');
      const prevButton = document.querySelector('.carousel-arrow-left');
      const nextButton = document.querySelector('.carousel-arrow-right');

      if (carousel && prevButton && nextButton) {
        const scrollTestimonials = (direction) => {
          const card = carousel.querySelector('.testimonial-card');
          const cardWidth = card ? card.getBoundingClientRect().width : 280;
          const gap = 22;

          carousel.scrollBy({
            left: direction * (cardWidth + gap),
            behavior: 'smooth',
          });
        };

        prevButton.addEventListener('click', () => scrollTestimonials(-1));
        nextButton.addEventListener('click', () => scrollTestimonials(1));
      }
    </script>
  </body>
</html>

<style>
  :root {
    font-family:
      Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
      "Segoe UI", sans-serif;
    color: #17204A;
    background: #EAFBFF;
  }

  body {
    margin: 0;
    background:
      radial-gradient(circle at 10% 10%, rgba(0, 200, 255, 0.08), transparent 30%),
      radial-gradient(circle at 90% 4%, rgba(24, 214, 201, 0.08), transparent 26%),
      linear-gradient(180deg, #EAFBFF 0%, #F4FDFF 50%, #EEE8FF 100%);
  }

  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 150px 8% 90px;
    box-sizing: border-box;
    overflow: hidden;
    isolation: isolate;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background-image: url("/images/jagsy-joy-hero.png");
    background-size: cover;
    background-position: center right;
    background-repeat: no-repeat;
    z-index: 0;
    animation: heroZoom 16s ease-out forwards;
    transform-origin: center right;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(234, 251, 255, 0.82) 0%,
        rgba(234, 251, 255, 0.58) 35%,
        rgba(234, 251, 255, 0.18) 62%,
        rgba(234, 251, 255, 0.02) 100%
      );
    z-index: 1;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 690px;
  }

  .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: 13px;
    font-weight: 900;
    color: #17204A;
    margin: 0 0 16px;
  }

  .hero-content .eyebrow {
    color: #17204A;
  }

  .hero-title {
    font-size: clamp(64px, 8vw, 118px);
    line-height: 0.92;
    letter-spacing: -0.055em;
    margin: 0;
    color: #17204A;
    font-weight: 900;
    -webkit-text-fill-color: #17204A;
    background: none;
    background-image: none;
    -webkit-background-clip: initial;
    background-clip: initial;
    text-shadow: none;
  }

  h2 {
    font-size: clamp(42px, 5vw, 72px);
    line-height: 0.98;
    letter-spacing: -0.055em;
    margin: 0 0 22px;
    color: #17204A;
    font-weight: 900;
  }

  .hero-text {
    font-size: 16px;
    line-height: 1.7;
    max-width: 620px;
    color: #5f526b;
    margin: 24px 0;
  }

  .hero-actions {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 52px;
    padding: 0 24px;
    border-radius: 999px;
    text-decoration: none;
    font-weight: 900;
    transition:
      transform 0.22s ease,
      box-shadow 0.22s ease,
      opacity 0.22s ease;
  }

  .button:hover {
    transform: translateY(-3px);
  }

  .primary {
    background: linear-gradient(135deg, #00C8FF, #18D6C9);
    color: white;
    box-shadow: 0 14px 30px rgba(0, 200, 255, 0.24);
  }

  .primary:hover {
    box-shadow: 0 18px 40px rgba(0, 200, 255, 0.28);
  }

  .secondary {
    background: rgba(255, 255, 255, 0.88);
    color: #17204A;
    border: 1px solid #D1EFFF;
  }

  .animate {
    opacity: 0;
    transform: translateY(18px);
    animation: fadeSlideUp 0.9s ease forwards;
  }

  .delay-1 {
    animation-delay: 0.12s;
  }

  .delay-2 {
    animation-delay: 0.26s;
  }

  .delay-3 {
    animation-delay: 0.42s;
  }

  .delay-4 {
    animation-delay: 0.58s;
  }

  .section {
    padding: 90px 8%;
  }

  .section-heading {
    max-width: 920px;
    margin: 0 auto 52px;
    text-align: center;
    padding: 0 8%;
  }

  .section-heading .eyebrow {
    margin-bottom: 16px;
    opacity: 1;
    animation: none;
    transform: none;
  }

  .section-heading h2 {
    text-align: center;
  }

  .section-heading p {
    color: #5f526b;
    font-size: 16px;
    line-height: 1.7;
    margin: 0 auto;
    max-width: 820px;
  }

  .product-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 620px));
    justify-content: center;
    gap: 40px;
    margin-top: 48px;
    width: 100%;
    max-width: 1320px;
    margin-left: auto;
    margin-right: auto;
  }

  .skills-section {
    padding: 96px 8%;
    background:
      radial-gradient(circle at 12% 20%, rgba(0, 200, 255, 0.08), transparent 30%),
      radial-gradient(circle at 88% 8%, rgba(24, 214, 201, 0.08), transparent 28%),
      #EAFBFF;
  }

  .skills-image-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 32px;
    max-width: 1380px;
    margin: 0 auto;
  }

  .skills-image-card {
    margin: 0;
    border-radius: 34px;
    border: 1px solid #D1EFFF;
    background: rgba(255, 255, 255, 0.9);
    box-shadow: 0 24px 68px rgba(0, 200, 255, 0.08);
    overflow: hidden;
  }

  .skills-image-card img {
    width: 100%;
    height: auto;
    display: block;
    object-fit: contain;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
    max-width: 1440px;
    margin: 0 auto;
  }

  .feature-card {
    background: white;
    border: 1px solid #D1EFFF;
    border-radius: 32px;
    padding: 40px 30px;
    text-align: center;
    box-shadow: 0 16px 40px rgba(0, 200, 255, 0.04);
    transition: transform 0.22s ease, box-shadow 0.22s ease;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 50px rgba(0, 200, 255, 0.08);
  }

  .feature-icon {
    font-size: 42px;
    margin-bottom: 24px;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 80px;
    width: 80px;
    background: #F4FDFF;
    border-radius: 50%;
    margin-left: auto;
    margin-right: auto;
    border: 1px solid #EAFBFF;
  }

  .feature-card h3 {
    color: #17204A;
    font-size: 20px;
    font-weight: 900;
    margin-bottom: 16px;
    line-height: 1.2;
  }

  .feature-card p {
    color: #5f526b;
    font-size: 16px;
    line-height: 1.6;
    margin: 0;
  }

  .testimonials-section {
    padding: 84px 0 92px;
    background:
      radial-gradient(circle at 12% 18%, rgba(0, 200, 255, 0.08), transparent 28%),
      radial-gradient(circle at 88% 12%, rgba(24, 214, 201, 0.08), transparent 28%),
      #F4FDFF;
    overflow: hidden;
  }

  .testimonial-carousel-wrap {
    position: relative;
    width: 100%;
    overflow: hidden;
  }

  .testimonial-carousel-wrap::before,
  .testimonial-carousel-wrap::after {
    content: "";
    position: absolute;
    top: 0;
    bottom: 0;
    width: 120px;
    z-index: 4;
    pointer-events: none;
  }

  .testimonial-carousel-wrap::before {
    left: 0;
    background: linear-gradient(
      90deg,
      #F4FDFF 0%,
      rgba(244, 253, 255, 0.82) 38%,
      rgba(244, 253, 255, 0) 100%
    );
  }

  .testimonial-carousel-wrap::after {
    right: 0;
    background: linear-gradient(
      270deg,
      #F4FDFF 0%,
      rgba(244, 253, 255, 0.82) 38%,
      rgba(244, 253, 255, 0) 100%
    );
  }

  .testimonial-carousel {
    display: flex;
    gap: 22px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    padding: 8px 8% 18px;
    scrollbar-width: none;
    scroll-behavior: smooth;
  }

  .testimonial-carousel::-webkit-scrollbar {
    display: none;
  }

  .testimonial-card {
    flex: 0 0 280px;
    scroll-snap-align: start;
    background:
      radial-gradient(circle at 100% 0%, rgba(0, 200, 255, 0.05), transparent 34%),
      #ffffff;
    border: 1px solid #D1EFFF;
    border-radius: 28px;
    padding: 28px 24px;
    text-align: center;
    box-shadow: 0 18px 46px rgba(0, 200, 255, 0.06);
  }

  .testimonial-avatar {
    width: 82px;
    height: 82px;
    display: block;
    object-fit: cover;
    border-radius: 50%;
    margin: 0 auto 16px;
    border: 4px solid #ffffff;
    box-sizing: border-box;
    box-shadow:
      0 0 0 2px #D1EFFF,
      0 12px 28px rgba(0, 200, 255, 0.12);
    background:
      linear-gradient(135deg, #EAFBFF, #EEE8FF);
  }

  .stars {
    color: #18D6C9;
    font-size: 14px;
    letter-spacing: 0.08em;
    margin: 0 0 14px;
  }

  .review {
    color: #5f526b;
    font-size: 16px;
    line-height: 1.7;
    margin: 0 0 18px;
  }

  .customer {
    color: #17204A;
    font-size: 15px;
    font-weight: 900;
    margin: 0;
  }

  .carousel-arrow {
    position: absolute !important;
    top: 50% !important;
    z-index: 10 !important;

    width: 52px !important;
    height: 52px !important;

    display: flex !important;
    align-items: center !important;
    justify-content: center !important;

    border: 0 !important;
    border-radius: 999px !important;

    background: #00C8FF !important;
    color: #ffffff !important;

    font-size: 36px !important;
    line-height: 1 !important;
    font-weight: 900 !important;

    cursor: pointer !important;
    box-shadow: 0 16px 38px rgba(0, 200, 255, 0.24) !important;

    transform: translateY(-50%) !important;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease,
      opacity 0.2s ease !important;
  }

  .carousel-arrow:hover {
    transform: translateY(-50%) scale(1.06) !important;
    box-shadow: 0 20px 48px rgba(0, 200, 255, 0.32) !important;
  }

  .carousel-arrow-left {
    left: 32px !important;
  }

  .carousel-arrow-right {
    right: 32px !important;
  }

  .cta {
    text-align: center;
    background:
      radial-gradient(circle at 20% 20%, rgba(0, 200, 255, 0.12), transparent 30%),
      radial-gradient(circle at 80% 10%, rgba(24, 214, 201, 0.1), transparent 28%),
      white;
    margin: 40px 8% 80px;
    padding: 76px 8%;
    border-radius: 42px;
    border: 1px solid #D1EFFF;
    box-shadow: 0 28px 70px rgba(0, 200, 255, 0.08);
  }

  .cta h2 {
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  @keyframes fadeSlideUp {
    from {
      opacity: 0;
      transform: translateY(18px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes heroZoom {
    from {
      transform: scale(1.04);
    }

    to {
      transform: scale(1);
    }
  }

  @media (max-width: 920px) {
    .hero {
      min-height: auto;
      padding: 120px 6% 70px;
    }

    .hero-bg {
      background-position: center right;
    }

    .hero-overlay {
      background:
        linear-gradient(
          180deg,
          rgba(234, 251, 255, 0.94) 0%,
          rgba(234, 251, 255, 0.82) 54%,
          rgba(234, 251, 255, 0.5) 100%
        );
    }

    .hero-title {
      font-size: clamp(48px, 14vw, 76px);
      line-height: 0.95;
      letter-spacing: -0.045em;
    }

    h2 {
      font-size: clamp(34px, 9vw, 48px);
      line-height: 1;
    }

    .features-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
    }

    .product-grid,
    .skills-image-grid {
      grid-template-columns: 1fr;
    }

    .skills-section {
      padding: 72px 6%;
    }

    .skills-heading {
      margin-bottom: 34px;
    }

    .skills-image-grid {
      gap: 22px;
    }

    .skills-image-card {
      border-radius: 26px;
    }

    .testimonials-section {
      padding: 68px 0 76px;
    }

    .testimonial-heading {
      padding: 0 6%;
      margin-bottom: 30px;
    }

    .testimonial-carousel {
      padding: 6px 6% 16px;
      gap: 18px;
    }

    .testimonial-card {
      flex-basis: 255px;
      padding: 24px 20px;
      border-radius: 24px;
    }

    .testimonial-avatar {
      width: 74px;
      height: 74px;
    }

    .testimonial-carousel-wrap::before,
    .testimonial-carousel-wrap::after {
      width: 68px;
    }

    .carousel-arrow {
      width: 42px !important;
      height: 42px !important;
      font-size: 30px !important;
    }

    .carousel-arrow-left {
      left: 12px !important;
    }

    .carousel-arrow-right {
      right: 12px !important;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-bg,
    .animate,
    .button {
      animation: none;
      transition: none;
      transform: none;
      opacity: 1;
    }
  }
</style>>>>>>>>>